[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "print_pro"
authors = [
    {name = "Antigravity AI", email = "your-email@example.com"}
]
description = "A bundle of pre-configured print formats for easy distribution."
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "frappe"
]

[project.urls]
Homepage = "https://github.com/yourusername/print_pro"
