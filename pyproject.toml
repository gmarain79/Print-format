[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "print_pro"
version = "0.1.0"
authors = [
    {name = "Antigravity AI", email = "your-email@example.com"}
]
description = "A bundle of pre-configured, Arab App styled print formats for Frappe / ERPNext."
readme = "README.md"
dependencies = [
    "frappe"
]

[project.urls]
Homepage = "https://github.com/yourusername/print_pro"
